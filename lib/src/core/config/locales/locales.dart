import 'dart:ui';

class AppLocales {
  static const pathToLocales = 'assets/locales';

  static final supportedLocales = [
    const Locale('en'),
  ];

  static final startLocale = supportedLocales.first;
}
